{
  "name": "chatasset-server",
  "version": "0.1.0",
  "private": true,
  "description": "ChatAsset MVP server: accepts questions captured by the Chrome extension and appends them to a local JSON file. No external dependencies.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  }
}
